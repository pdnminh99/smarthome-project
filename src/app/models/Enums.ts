export enum Theme {
  DARK,
  LIGHT
}

export enum Status {
  OFF,
  SAFE,
  DISCONNECT,
  SMOKE,
  FIRE
}

export enum Motion {
  DISCONNECT,
  NO_MOTION,
  MOTION
}

export enum ScreenSize {
  SMALL,
  MEDIUM,
  BIG
}
